# Loads data/catalog.json into the demo database. Run: python -m src.db.seed
# TODO: create tables, then insert products (idempotently, so re-running is safe).

def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
