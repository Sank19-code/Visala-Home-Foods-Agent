# SQLAlchemy models: Product, Cart, CartItem, Order, Payment, AuditEvent, IdempotencyRecord.
# TODO: define tables.
# Order needs at minimum: id, cart_id, amount_paise, status, idempotency_key (unique),
# razorpay_order_id, payment_link_url, created_at.
